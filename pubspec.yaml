name: flutter_artist_vibe_theme
description: "Flutter Artist Vibe Theme"
version: 0.0.1
resolution: workspace
homepage:

environment:
  sdk: ^3.11.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_artist_core: ^0.9.1
  flutter_artist_styles: ^0.9.1+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  
flutter:

# -------------------------------------------------------
# dependency_overrides:
# -------------------------------------------------------